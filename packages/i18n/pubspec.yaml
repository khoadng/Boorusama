name: i18n
description: A package for managing internationalization in Boorusama.
publish_to: none

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  collection: ^1.19.1
  equatable: ^2.0.5
  intl: ^0.20.2
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  timeago: ^3.7.0
  slang:
    git:
      url: https://github.com/khoadng/slang.git
      path: slang
      ref: fork
  slang_flutter:
    git:
      url: https://github.com/khoadng/slang.git
      path: slang_flutter
      ref: fork

dev_dependencies:
  codegen:
    path: ../codegen
  flutter_lints: ^6.0.0

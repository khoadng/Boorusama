enum DmailFolderType {
  all,
  received,
  unread,
  sent,
  deleted,
}

enum ArtistOrder {
  recentCreated,
  lastUpdated,
  name,
  count,
}

enum UploadOrder {
  id,
  idAsc,
  random,
}

enum UploadStatus {
  pending,
  completed,
  error,
}

enum ReportPeriod {
  day,
  week,
  month,
  year,
}

enum TopicOrder {
  sticky,
}

enum ImageQuality {
  automatic,
  low,
  high,
  original,
}

enum GridSize {
  small,
  normal,
  large,
}

enum ImageListType {
  standard,
  masonry,
}

enum DataCollectingStatus {
  allow,
  prohibit,
}

enum ActionBarDisplayBehavior {
  scrolling,
  staticAtBottom,
}

enum DetailsDisplay {
  postFocus,
  imageFocus,
}

enum ContentOrganizationCategory {
  infiniteScroll,
  pagination,
}
